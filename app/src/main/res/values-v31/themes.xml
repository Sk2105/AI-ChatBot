<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AIBotApp" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Drawable to be rendered in window's background
             while the activity is being loaded -->
        <item name="android:windowSplashScreenAnimatedIcon">
            @mipmap/ic_launcher
        </item>
        <item name="android:windowSplashScreenAnimationDuration">
            2000
        </item>
        <item name="android:windowEnterAnimation">
            @android:anim/fade_in
        </item>

        <item name="android:windowExitAnimation">
            @android:anim/fade_out
        </item>

        <item name="android:windowBackground">?attr/windowSplashScreenBackground</item>



    </style>
</resources>